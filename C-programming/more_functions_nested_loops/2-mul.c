#include "main.h"
#include <stdio.h>
/**
 * main - multiplies two integers
 * returns the product of the 
 * two integers a and b
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
