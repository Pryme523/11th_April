#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the alphabet in lower case
 *
 * Return 0 Always
 */

int main (void)
{
	char a = 'a';
		while(a <= 'z')
		{
			putchar(a);
			a++;
		}
	return (0);
}
